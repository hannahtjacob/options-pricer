[project]
name = "options-pricer"
version = "0.1.0"
description = "A Python options pricing engine with Black-Scholes, Greeks, Monte Carlo simulation, implied volatility, and Streamlit dashboard."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]